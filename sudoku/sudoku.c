#include "sudoku.h"

int *generateGridArray()
{
    for (int i = 0; i < 9; i++)
    {
        for (int j = 0; j < )
    }
}

void play()
{
    int grid[81] = generateGridArray();
    printGrid(grid);
}